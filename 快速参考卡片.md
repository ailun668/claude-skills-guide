# Claude Skills 快速参考卡片

> 📋 一页纸速查手册 - 打印或保存以便快速查阅

---

## 🎯 核心概念

### Skills 是什么？
标准化的能力包，将专家知识封装成可复用模块，让 Claude 成为领域专家。

### 三层结构
```
skill-name/
├── SKILL.md          # SOP - 标准作业程序
├── scripts/          # Tools - 可执行脚本
└── reference/        # Resources - 参考资料
```

### 渐进式披露
- **Level 1**: name + description (~100 tokens) - 始终加载
- **Level 2**: SKILL.md 主体 (~5k tokens) - 触发时加载
- **Level 3**: scripts + reference - 按需加载

---

## 📝 SKILL.md 模板

```yaml
---
name: skill-name                    # 小写+连字符，最大64字符
description: 做什么 + 何时用 + 触发词  # 最大1024字符
---

# Skill Title

简短概述（1-2句话）

## Capabilities
- 能力1
- 能力2

## How to Use
1. 步骤1
2. 步骤2

## Example Usage
"示例请求1"
"示例请求2"

## Scripts
- `script.py`: 描述

## Limitations
- 限制1
```

---

## 🐍 Python 脚本模板

```python
#!/usr/bin/env python3
import sys
import json

def main():
    try:
        # 读取输入
        input_data = json.loads(sys.stdin.read())
        
        # 处理数据
        result = {"status": "success", "data": input_data}
        
        # 输出结果
        print(json.dumps(result, indent=2))
        
    except Exception as e:
        error = {
            "status": "error",
            "message": str(e)
        }
        print(json.dumps(error), file=sys.stderr)
        sys.exit(1)

if __name__ == "__main__":
    main()
```

---

## 🧪 测试脚本模板

```python
from anthropic import Anthropic
import os

client = Anthropic(api_key=os.getenv("ANTHROPIC_API_KEY"))

response = client.beta.messages.create(
    model="claude-sonnet-4-5-20250929",
    max_tokens=4096,
    betas=["code-execution-2025-08-25", "skills-2025-10-02"],
    container={
        "skills": [{"type": "local", "path": "skills/my-skill"}]
    },
    tools=[{"type": "code_execution_20250825", "name": "code_execution"}],
    messages=[{"role": "user", "content": "测试消息"}]
)
```

---

## ✅ 最佳实践清单

### Description 编写
- [ ] 包含"做什么"和"何时用"
- [ ] 包含关键触发词
- [ ] 精准，避免过度触发
- [ ] 最大 1024 字符

### SKILL.md 编写
- [ ] 保持简洁（< 500 行）
- [ ] 只教 Claude 不知道的
- [ ] 提供具体示例
- [ ] 使用清晰的步骤
- [ ] 详细文档放 reference/

### 脚本开发
- [ ] 完善的错误处理
- [ ] JSON 输入/输出
- [ ] 清晰的文档字符串
- [ ] 输入验证
- [ ] 调试信息输出到 stderr

### 测试
- [ ] 在 Haiku、Sonnet、Opus 上测试
- [ ] 测试错误处理
- [ ] 测量 token 使用
- [ ] 验证输出格式

### 文件规范
- [ ] 使用正斜杠 `/` 路径
- [ ] name 使用小写+连字符
- [ ] 脚本使用小写+下划线
- [ ] 不包含时效性信息
- [ ] 不硬编码敏感信息

---

## 🚫 常见错误

### ❌ 不要做
```yaml
# 太简短
description: Financial tool

# 太泛化
description: Helps with data

# 第一人称
description: I can analyze data

# Windows 路径
reference\guide.md

# 过度解释
Python is a programming language...
```

### ✅ 应该做
```yaml
# 精准描述
description: Calculate financial ratios (ROE, ROA, P/E) from statements. Use when analyzing company performance.

# Unix 路径
reference/guide.md

# 简洁直接
Use this formula: ROE = Net Income / Equity
```

---

## 🔧 命令速查

### 创建 Skill 结构
```bash
mkdir -p skills/my-skill/{scripts,reference}
touch skills/my-skill/SKILL.md
```

### 测试脚本独立运行
```bash
echo '{"test": "data"}' | python scripts/my_script.py
```

### 检查脚本权限
```bash
chmod +x scripts/my_script.py
```

### 测量文件行数
```bash
wc -l skills/my-skill/SKILL.md
```

---

## 📊 设计模式速查

### 工作流模式
```markdown
### Phase 1: Preparation
- [ ] 验证输入
- [ ] 创建备份

### Phase 2: Processing
- [ ] 处理数据

### Phase 3: Validation
- [ ] 运行验证脚本
- [ ] 如果失败，返回 Phase 2

**反馈循环**: 执行 → 验证 → 修复 → 重复
```

### 决策树模式
```markdown
1. 检查输入格式:
   - 如果 CSV → 使用 process_csv.py
   - 如果 JSON → 使用 process_json.py
   - 如果其他 → 先转换为 CSV
```

### 验证模式
```markdown
## 输出验证清单
- [ ] 所有必需字段存在
- [ ] 数据类型正确
- [ ] 计算结果准确
- [ ] 格式一致
```

---

## 🎨 自由度设定

| 任务脆弱性 | 自由度 | 指令类型 | 示例 |
|-----------|--------|---------|------|
| 低 | 高 | 文本指导 | "审查代码质量" |
| 中 | 中 | 伪代码/模板 | "使用此模板并自定义" |
| 高 | 低 | 精确脚本 | "运行: `python migrate.py`" |

---

## 🐛 故障排除

### Skill 未触发
- 检查 description 是否包含触发词
- 验证 YAML frontmatter 格式
- 测试用户请求是否匹配

### 脚本执行失败
```bash
# 检查权限
chmod +x scripts/script.py

# 独立测试
echo '{}' | python scripts/script.py

# 检查依赖
pip list | grep package_name
```

### 输出格式错误
```python
# 正确：只输出 JSON 到 stdout
print(json.dumps(result))

# 正确：调试信息到 stderr
print("[DEBUG]", file=sys.stderr)

# 错误：混合输出
print("Debug info")  # 会破坏 JSON
print(json.dumps(result))
```

---

## 📈 性能优化

### Token 优化
- ✅ 精准的 description
- ✅ 简洁的 SKILL.md
- ✅ 详细文档放 reference/
- ✅ 使用脚本处理确定性任务

### 模型选择
- **Haiku**: 简单任务、高频调用、成本敏感
- **Sonnet**: 大多数生产场景、平衡性能
- **Opus**: 复杂推理、关键任务、最高质量

---

## 🔗 API 使用

### 本地 Skill
```python
container={
    "skills": [
        {"type": "local", "path": "skills/my-skill"}
    ]
}
```

### 内置 Skill
```python
container={
    "skills": [
        {"type": "anthropic", "skill_id": "xlsx", "version": "latest"}
    ]
}
```

### 混合使用
```python
container={
    "skills": [
        {"type": "anthropic", "skill_id": "xlsx", "version": "latest"},
        {"type": "local", "path": "skills/my-skill"}
    ]
}
```

---

## 📦 文件下载

```python
# 提取 file_id
import re
file_ids = re.findall(r'file_[a-zA-Z0-9]+', str(response))

# 下载文件
for file_id in file_ids:
    metadata = client.beta.files.retrieve_metadata(file_id=file_id)
    content = client.beta.files.download(file_id=file_id)
    
    with open(f"outputs/{metadata.filename}", "wb") as f:
        f.write(content.read())
```

---

## 💰 成本估算

### 定价（每百万 tokens）

| 模型 | 输入 | 输出 |
|------|------|------|
| Haiku | $0.25 | $1.25 |
| Sonnet | $3.00 | $15.00 |
| Opus | $15.00 | $75.00 |

### 计算成本
```python
input_cost = (input_tokens / 1_000_000) * price_per_m
output_cost = (output_tokens / 1_000_000) * price_per_m
total = input_cost + output_cost
```

---

## 📚 学习资源

### 必读文档
- [完全指南](./Claude_Skills_开发完全指南.md) - 2500+ 行详细教程
- [官方最佳实践](https://docs.claude.com/en/docs/agents-and-tools/agent-skills/best-practices)
- [官方示例](./claude-cookbooks/skills/)

### 快速链接
- [Skills Overview](https://docs.claude.com/en/docs/agents-and-tools/agent-skills/overview)
- [Quickstart](https://docs.claude.com/en/docs/agents-and-tools/agent-skills/quickstart)
- [GitHub Cookbook](https://github.com/anthropics/claude-cookbooks/tree/main/skills)

---

## ⚡ 快速开始流程

```
1. 创建目录结构
   ↓
2. 编写 SKILL.md（使用模板）
   ↓
3. 开发脚本（如需要）
   ↓
4. 添加参考资料（如需要）
   ↓
5. 本地测试
   ↓
6. 多模型测试
   ↓
7. 性能优化
   ↓
8. 部署使用
```

---

## 🎯 核心原则（记住这6条）

1. **简洁为王** - 只教 Claude 不知道的
2. **渐进式披露** - 分层组织信息
3. **适度自由** - 根据任务设定严格程度
4. **全模型测试** - Haiku、Sonnet、Opus
5. **结构化思维** - 清晰的步骤和反馈循环
6. **迭代开发** - 与 Claude 共创，持续优化

---

**💡 提示**: 将此卡片打印或保存为 PDF，开发时随时查阅！

**📖 详细内容**: 查看 [Claude_Skills_开发完全指南.md](./Claude_Skills_开发完全指南.md)

**🚀 开始创建你的第一个 Skill！**

